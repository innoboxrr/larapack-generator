<?php

namespace Namespace\Http\Controllers;

use Illuminate\Routing\Controllers\HasMiddleware;
use Illuminate\Routing\Controllers\Middleware;
use Namespace\Http\Requests\PascalCaseModelName\{
    // @larapack:if policies
    PoliciesRequest,
    // @larapack:endif
    // @larapack:if policy
    PolicyRequest,
    // @larapack:endif
    // @larapack:if index
    IndexRequest,
    // @larapack:endif
    // @larapack:if show
    ShowRequest,
    // @larapack:endif
    // @larapack:if create
    CreateRequest,
    // @larapack:endif
    // @larapack:if update
    UpdateRequest,
    // @larapack:endif
    // @larapack:if delete
    DeleteRequest,
    // @larapack:endif
    // @larapack:if restore
    RestoreRequest,
    // @larapack:endif
    // @larapack:if forceDelete
    ForceDeleteRequest,
    // @larapack:endif
    // @larapack:if export
    ExportRequest
    // @larapack:endif
};

class PascalCaseModelNameController extends Controller implements HasMiddleware
{
    /**
     * Middleware aplicado a las acciones del controlador.
     *
     * @return array<int, \Illuminate\Routing\Controllers\Middleware|string>
     */
    public static function middleware(): array
    {
        return [
            new Middleware('auth:sanctum'),
        ];
    }
    // @larapack:if policies

    public function policies(PoliciesRequest $request)
    {
        return $request->handle($this);
    }
    // @larapack:endif
    // @larapack:if policy

    public function policy(PolicyRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if index

    public function index(IndexRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if show

    public function show(ShowRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if create

    public function create(CreateRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if update

    public function update(UpdateRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if delete

    public function delete(DeleteRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if restore

    public function restore(RestoreRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if forceDelete

    public function forceDelete(ForceDeleteRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
    // @larapack:if export

    public function export(ExportRequest $request)
    {
        return $request->handle();
    }
    // @larapack:endif
}
