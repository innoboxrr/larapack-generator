<?php

namespace Namespace\Tests\Feature\Models;

use Illuminate\Support\Facades\Auth;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Foundation\Testing\WithFaker;
use Tests\TestCase;

class PascalCaseModelNameEndpointsTest extends TestCase
{

    use RefreshDatabase,
        WithFaker;

    // @larapack:if policies
    public function test_snake_case_model_name_policies_endpoint(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();
        
        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'id' => $camelCaseModelName->id
        ];

        $this->json('GET', '/api/kebabcasemodelname/policies', $payload, $headers)
            ->assertStatus(200);

    }

    // @larapack:endif
    // @larapack:if policy
    public function test_snake_case_model_name_policy_endpoint(): void
    {
        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'policy' => 'index'
        ];

        $this->json('GET', '/api/kebabcasemodelname/policy', $payload, $headers)
            ->assertJsonStructure([
                'index'
            ])
            ->assertStatus(200);

    }

    // @larapack:endif
    // @larapack:if index
    public function test_snake_case_model_name_index_auth_endpoint(): void
    {

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'managed' => true
        ];

        $this->json('GET', '/api/kebabcasemodelname/index', $payload, $headers)
            ->assertStatus(200);

    }

    // @larapack:endif
    // @larapack:if index
    public function test_snake_case_model_name_index_guest_endpoint(): void
    {

        Auth::guard('web')->logout();

        $headers = [
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'managed' => true
        ];

        $this->json('GET', '/api/kebabcasemodelname/index', $payload, $headers)
            ->assertStatus(401);
            
    }
    
    // @larapack:endif
    // @larapack:if show
    public function test_snake_case_model_name_show_auth_endpoint(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'snake_case_model_name_id' => $camelCaseModelName->id
        ];

        $this->json('GET', '/api/kebabcasemodelname/show', $payload, $headers)
            ->assertStatus(200);
            
    }

    // @larapack:endif
    // @larapack:if show
    public function test_snake_case_model_name_show_guest_endpoint(): void
    {

        Auth::guard('web')->logout();
        
        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $headers = [
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'snake_case_model_name_id' => $camelCaseModelName->id
        ];

        $this->json('GET', '/api/kebabcasemodelname/show', $payload, $headers)
            ->assertStatus(401);
            
    }

    // @larapack:endif
    // @larapack:if create
    public function test_snake_case_model_name_create_endpoint(): void
    {

        $user = \Namespace\Models\User::first();

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = \Namespace\Models\PascalCaseModelName::factory()->make()->getAttributes();

        $this->json('POST', '/api/kebabcasemodelname/create', $payload, $headers)
            ->assertStatus(201);
            
    }

    // @larapack:endif
    // @larapack:if update
    public function test_snake_case_model_name_update_endpoint(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            ...\Namespace\Models\PascalCaseModelName::factory()->make()->getAttributes(),
            'snake_case_model_name_id' => $camelCaseModelName->id
        ];

        $this->json('PUT', '/api/kebabcasemodelname/update', $payload, $headers)
            ->assertStatus(200);
            
    }

    // @larapack:endif
    // @larapack:if delete
    public function test_snake_case_model_name_delete_endpoint(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'snake_case_model_name_id' => $camelCaseModelName->id
        ];

        $this->json('DELETE', '/api/kebabcasemodelname/delete', $payload, $headers)
            ->assertStatus(200);
            
    }

    // @larapack:endif
    // @larapack:if restore
    public function test_snake_case_model_name_restore_endpoint(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'snake_case_model_name_id' => $camelCaseModelName->id
        ];

        $this->json('POST', '/api/kebabcasemodelname/restore', $payload, $headers)
            ->assertStatus(200);
            
    }

    // @larapack:endif
    // @larapack:if forceDelete
    public function test_snake_case_model_name_force_delete_endpoint(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            'snake_case_model_name_id' => $camelCaseModelName->id
        ];

        $this->json('DELETE', '/api/kebabcasemodelname/force-delete', $payload, $headers)
            ->assertStatus(403);
            
    }

    // @larapack:endif
    // @larapack:if export
    public function test_snake_case_model_name_export_endpoint(): void
    {   

        $headers = [
            // 'Authorization' => config('test.token'),
            'Content-Type' => 'application/json',
            'Accept' => 'application/json'
        ];  

        $payload = [
            //
        ];

        $this->json('POST', '/api/kebabcasemodelname/export', $payload, $headers)
            ->assertStatus(200);
            
    }

    // @larapack:endif
    // @larapack:if immutable
    public function test_snake_case_model_name_no_expone_rutas_de_escritura(): void
    {

        foreach (['update', 'delete', 'restore', 'force.delete'] as $action) {
            $this->assertFalse(
                \Illuminate\Support\Facades\Route::has('api.dotNamespacesnake_case_model_name.' . $action),
                "PascalCaseModelName es inmutable y tiene registrada la ruta {$action}."
            );
        }

    }

    public function test_snake_case_model_name_no_admite_modificaciones(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $this->expectException(\LogicException::class);

        $camelCaseModelName->touch();

    }

    public function test_snake_case_model_name_no_admite_borrarse(): void
    {

        $camelCaseModelName = \Namespace\Models\PascalCaseModelName::factory()->create();

        $this->expectException(\LogicException::class);

        $camelCaseModelName->delete();

    }

    // @larapack:endif
}
