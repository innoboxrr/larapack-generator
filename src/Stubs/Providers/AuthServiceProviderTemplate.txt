<?php

namespace Namespace\Providers;

use Illuminate\Support\Facades\Gate;
use Illuminate\Support\ServiceProvider;

class AuthServiceProvider extends ServiceProvider
{

    /**
     * Las políticas de los modelos ya no se registran aquí: cada modelo declara
     * la suya con el atributo #[UsePolicy(FooPolicy::class)], que Laravel resuelve
     * automáticamente. Este proveedor queda para gates y habilidades sueltas.
     */
    public function boot(): void
    {
        $this->defineGates();
    }

    protected function defineGates(): void
    {
        // Gate::define('nombre-de-la-habilidad', function ($user) {
        //     return $user->isAdmin();
        // });
    }

}
